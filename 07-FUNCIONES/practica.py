def saludar ():
    nombre = input("¿Cómo te llamas? ")
    print(str ("¡Hola, bienvenido a la clase de funciones en Python! " + nombre))


saludar()
saludar()
saludar()

#LLamo 3 veces y me pregunta el nombre 3 veces, cada vez que lo llamo se ejecuta el código dentro de la función.interesante
# solo llame a la funcion le agregue nombre sin parametros escribi codigo dentro de la funcion y se ejecuta cada vez que la llamo, 
# es como una receta que puedo usar cada vez que quiera sin reescribirla."""